version: '2'

services:

  owncloud:
    image: owncloud
    restart: always
    ports:
      - 80:80
    volumes:
      - /var/www/html/data/admin/files
    environment:
      - OWNCLOUD_ADMIN_USERNAME=admin
      - OWNCLOUD_ADMIN_PASSWORD=admin
