version: '2'

services:

  owncloud:
    image: owncloud
    restart: always
    ports:
      - 80:80
    volumes:
      - /mnt/data/files/admin/files
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin
      - SERVER_NAME='$(APPNAME).insights-lab.com'
